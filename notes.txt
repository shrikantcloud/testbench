docker run --name mysqldb --network springboot-mysql-net -e MYSQL_ROOT_PASSWORD=root -e MYSQL_DATABASE=testbench -d mysql

docker exec -it mysqldb bash

mysql -u root -p

docker build -t testbench-app


docker run --network springboot-mysql-net --name springboot-mysql-container -p 8080:8080 testbench-app